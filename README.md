# Cosmic Ripple Interaction

A single-page interactive ripple field with pointer control and optional camera-based hand gesture tracking.

## Run locally

```bash
python3 -m http.server 8123
```

Then open `http://localhost:8123/`.

